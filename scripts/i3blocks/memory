#!/usr/bin/env bash
set -euo pipefail

free | awk '
  /Mem:/ {
    usage = int(($3 / $2) * 100 + 0.5)
    color = "#22c55e"
    if (usage >= 85) {
      color = "#ef4444"
    } else if (usage >= 60) {
      color = "#eab308"
    }
    printf "R %d%%\n\n%s\n", usage, color
  }
'
